fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy==2.0.35
streamlit==1.39.0
httpx==0.27.2
python-dotenv==1.0.1  # not imported directly; pydantic-settings requires it for env_file loading
plotly==5.24.1
pandas==2.2.3
# Pinned explicitly, not left as transitive deps of pandas/streamlit: an
# unpinned numpy resolved to 2.4.6 against pyarrow 25.0.0, and that specific
# combination segfaults inside pyarrow's internal pandas_compat.convert_column
# (the exact path Streamlit's st.dataframe() uses) — confirmed via
# PYTHONFAULTHANDLER=1 native traceback, not a version-number guess. These
# versions are the ones actually exercised together at streamlit==1.39.0's
# release and are confirmed crash-free against the same repro.
numpy==1.26.4
pyarrow==17.0.0
pyyaml==6.0.2
google-generativeai==0.8.3
pytest==8.3.3
psycopg[binary]==3.2.3
scikit-learn==1.5.2
torch==2.5.1
transformers==4.57.6
sentence-transformers==3.2.1
